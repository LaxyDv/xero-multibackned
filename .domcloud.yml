features:
  - python 3.11

nginx:
  passenger:
    enabled: "on"
    app_start_command: "uvicorn main:app --host 0.0.0.0 --port $PORT"

commands:
  - pip install --upgrade pip
  - pip install -r requirements.txt
